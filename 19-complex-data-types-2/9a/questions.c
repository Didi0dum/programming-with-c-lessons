#include <stdio.h>

float sum(int a, int b) {
  return a + b;
}

typedef float (*fooptr)(int, int);

enum Workday {
  MONDAY = 1,
  TUESDAY,
  WEDNESDAY,
  THURSDAY = 7,
  FRIDAY
};

union test {
  int x;
  double y;
};

int main(void) {

  

  return 0;
}